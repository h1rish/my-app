pipeline {
  agent any
  stages {
    stage('hai') {
      steps {
        echo " Running ${JOB_NAME} ON ${JENKINS_URL} "
    
       }
     }
      stage('docker build') {
      steps {
        
      }
    }
  }
}  
